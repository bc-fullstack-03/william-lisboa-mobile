import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginTop: 48,
  },
  containerPosition: {
    alignItems: 'center',
    marginTop: 0,
    paddingBottom: 24,
  },
  logo: {
    width: 100,
    height: 100,
  },
});